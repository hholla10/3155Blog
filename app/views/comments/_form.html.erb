<%= form_for([@post, @post.comments.build]) do |f| %>
<p>
    
    <%= f.label :name %>
</p>
<p>
    
<%= f.label :body %> <br>
<%= f.text_area :body %>
</p>
<p>
    
    <%= f.submit %>
</p>

<% end %>